#include <bits/stdc++.h>
using namespace std;

int h[200001], r[200001];

int main()
{
    int n, m;
    cin >> n >> m;
    for (int o = 1; o <= n; o++)
        cin >> h[o];
    for (int o = 1; o <= m; o++)
        cin >> r[o];
    return 0;
}